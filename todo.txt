Ruben
- configfile parsen

David
todo:
- errorCodes overal updaten https://developer.mozilla.org/en-US/docs/Web/HTTP/Status
- fileOpen algemene functie - waar definiëren? 
- File upload config Location
	-https://stackoverflow.com/questions/4526273/what-does-enctype-multipart-form-data-mean
- Directory listing - autoindex on/off - config location
- File deletion - DELETE method van config file
- http version check?
- chunked transfer

Alle
- CGI 
- Set POST data in the env of cgi
lezen: chunked transfer, 
?
- parse request
	- error codes
- parse response
- handle response

eval:
-Try a redirected URL.
-Try a wrong URL on the server.
-Stress tests your server. It must stay available at all cost. (siege - homebrew)
-Your server must be able to listen to multiple ports 
-read/recv/write/send and check if the returned value is correctly checked 
	(checking only -1 or 0 values is not enough, both should be checked).
	-chi
UNKNOWN requests should not result in a crash.

lijstje:
- algemene fileopen
- Errorcodes overal
- file upload - multiple files - https://stackoverflow.com/questions/4526273/what-does-enctype-multipart-form-data-mean
- flow mooier.
- chunked transfer encoding en tot content length lezen.
- upload werkend
- parse config
- delete met check config
- autoindex met on/off check